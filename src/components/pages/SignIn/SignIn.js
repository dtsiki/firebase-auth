import PropTypes from 'prop-types';
import React from 'react';

import './style.scss';

const SignIn = () => {
  return (
    <div>
      <h1>Sign In</h1>
    </div>
  );
};

SignIn.propTypes = {
  mode: PropTypes.string,
};

export default SignIn;
